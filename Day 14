#include <stdio.h>

int main() {
    int x;
    scanf("%d",&x);

    for(int i=0;i<x;i++) {
        int A,B;
        scanf("%d %d",&A,&B);

        if(B>A)
            printf("PROFIT
");
        else if(B<A)
            printf("LOSS
");
        else
            printf("NEUTRAL
");
    }
}
